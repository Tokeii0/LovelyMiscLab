# 2026-07-02

## 更新 README
- 项目 LovelyMiscLab：面向 CTF Misc 的可视化节点图工作台（本地版 CyberChef + 流程图）。
- 架构：Cargo workspace，`crates/misclab-core`（纯 Rust 引擎，Tauri 无关，可无头单测）+ `src-tauri`（薄适配层）+ `src/`（React 19 前端）。
- 关键点：NodeDescriptor 是数据驱动 UI 的单一真相源；100+ 内置节点；GraphExecutor 拓扑排序+内容寻址增量缓存；支持 AI 生成流程、复合模块、脚本节点。
- 前端栈：React19/Vite7/Tailwind v4/@xyflow/react/zustand/TanStack Query/React Router。
- 把原来的 Tauri 默认模板 README 全量替换为完整项目文档（含架构树、command 表、开发命令）。
- 测试命令：`cargo test -p misclab-core`；开发：`pnpm tauri dev`；浏览器预览：`pnpm dev`。

## README 二次修订
- 用户要求：不要出现 "CyberChef"（改为对标 ComfyUI 的节点式工作流）；顶部美化。
- 定位统一为「对标 ComfyUI 的节点式可视化工作台」——数据驱动、可视化编排、节点可扩展，把"生成图像"换成"Misc 分析"。
- 顶部加了居中标题(🧪 emoji)、副标题、slogan，以及 6 个 shields.io 徽章(Tauri/React/Rust/TS/Vite/MIT)。

